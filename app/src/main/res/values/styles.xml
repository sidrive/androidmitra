<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="RatingBar" parent="Theme.AppCompat">
    <item name="colorControlNormal">@color/goldenBrownLawyer</item>
    <item name="colorControlActivated">@color/goldenBrownLawyer</item>
  </style>

  <style name="AppTheme1" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="Apptheme2" parent="Theme.AppCompat.Light">
    <item name="colorAccent">@color/goldenBrownLawyer</item>
  </style>

  <style name="LoginTheme1" parent="Base.ThemeOverlay.AppCompat.Light">
    <item name="colorPrimary">@color/grayLawyer</item>
    <item name="colorPrimaryDark">@color/goldenBrownLawyer</item>
    <item name="colorAccent">@color/colorWhite</item>
    <item name="android:windowTranslucentStatus">true</item>
  </style>

  <style name="LoginTheme" parent="Base.ThemeOverlay.AppCompat.Dark">
    <item name="colorPrimary">@color/grayLawyer</item>
    <item name="colorPrimaryDark">@color/goldenBrownLawyer</item>
    <item name="colorAccent">@color/goldenBrownLawyer</item>
  </style>

  <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

  <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

  <style name="HistoryTabLayout" parent="Widget.Design.TabLayout">
    <item name="tabIndicatorColor">@color/colorPrimaryRedDark</item>
    <item name="tabIndicatorHeight">2dp</item>
    <item name="tabPaddingStart">12dp</item>
    <item name="windowActionBarOverlay">true</item>
    <item name="tabPaddingEnd">12dp</item>
    <item name="tabBackground">@android:color/white</item>
    <item name="tabTextAppearance">@style/HistoryTabTextAppearance</item>
    <item name="tabSelectedTextColor">@android:color/black</item>
  </style>

  <style name="HistoryTabTextAppearance" parent="TextAppearance.Design.Tab">
    <item name="android:textSize">16sp</item>
    <item name="android:textColor">@android:color/black</item>
    <item name="textAllCaps">true</item>
  </style>

  <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
    <!-- Used for the buttons -->
    <item name="colorAccent">#FFC107</item>
    <!-- Used for the title and text -->
    <item name="android:textColorPrimary">#000000</item>
    <!-- Used for the background -->
    <item name="android:background">#99ffffff</item>
  </style>

  <style name="DialogTheme" parent="android:Theme.Dialog">

    <item name="android:layout_width">fill_parent</item>
    <item name="android:layout_height">fill_parent</item>
    <!-- No backgrounds, titles or window float -->
    <item name="android:windowBackground">@null</item>
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowIsFloating">false</item>
  </style>

</resources>
